{
  "name": "portfolio",
  "version": "0.1.0",
  "description": "Portfolio built using Three.js that animates 3D elements as the user scrolls",
  "main": "index.js",
  "scripts": {
    "build": "parcel public/index.html",
    "lint": "eslint '*/**/*.{js,ts}' --fix",
    "prettier": "prettier --loglevel warn --write '**/*.{js,json,md,ts}'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.0.0-beta.2",
    "three": "^0.137.0"
  },
  "devDependencies": {
    "@types/three": "^0.128.0",
    "typescript": "^4.3.2"
  }
}
